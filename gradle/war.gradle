apply plugin: "war"

bootWar {
    mainClassName = "com.crosby.krakenreader.KrakeninterviewApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
